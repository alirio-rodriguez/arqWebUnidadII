spring:
  application:
    name: ArqWeb

  datasource:
    url: jdbc:h2:mem:polidb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 5400
